#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array

/*
    a tiles in one dir and b tiles in the other dir

    xk, yk and xq, yq 

    no of pos such that if knight was placed on that cell, then it attack king and queen both

    (xk, yk) != (xq, yq) 

    let's first see test case: 
    a = 1 and b = 1 
    xk, yk == 3, 1
    xq, yq == 1, 3 
    
*/  

void solve() {
    int a, b; cin >> a >> b;
    int xk, yk; cin >> xk >> yk;
    int xx, yy; cin >> xx >> yy;
    

}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t = 1;
    cin >> t;
    while(t--) solve();
}